function hello <- (hi, hello_person="hello") {
    .print <- ("Hello ", hello_person, "hi=", hi, sep="")
    .exit <- (2)
}

-> {
    someVal := True

    svc.fit <- ("", "")

    do {
        .hello <- ("hello")
    }
    if {
        someVal
    }
}