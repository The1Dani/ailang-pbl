function hello <- (hi, hello_person="hello") {
    .print <- ("Hello", hello_person)
    .print <- ("hi=", hi, sep="")
    .exit <- (2)
}

-> {
    someVal := True
    .print <- (None)

    do {
        .hello <- ("hello", hello_person="World")
    }
    if {
        someVal
    }
}